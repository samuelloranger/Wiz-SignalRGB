declare class UDPReqResClient {
    private client;
    private ip;
    private port;
    private requestTimeout;
    private retryCount;
    private counter;
    constructor(ip: string, port: number, requestTimeout: number, retryCount: number);
    request<T>(data: Record<string, unknown>): Promise<T>;
    close(): void;
    private withRetry;
    private basicRequest;
}
export { UDPReqResClient };
